---
import { getSessionCookie } from "../../lib/auth";

// Logout - clear cookie and redirect to landing page
Astro.cookies.delete(getSessionCookie(), { path: "/" });

return Astro.redirect("/");
---
